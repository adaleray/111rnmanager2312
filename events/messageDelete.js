module.exports.help = { name: "messageDelete" };

class MessageDelete {
  constructor(msg, sunucu) {
    this.msg = msg;
    this.sunucu = sunucu;
  }
  
  logla(kanal) {
    
  }
  
  snipe(db) {
    
  }
}

module.exports.event = () => {};