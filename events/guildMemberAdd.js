module.exports.event = async (uye, client = global.client, cfg = require("../config.json"), db = require("quick.db")) => {
  console.log("piç girdi");
 };

module.exports.help = { name: "guildMemberAdd" };